# Claude Instructions

Read the full workflow rules from `AGENTS.md` in this same directory before starting any task:

```
@AGENTS.md
```

If the `@` import is not supported in this context, read the file directly:
`/home/saldave/projects/.vscode/AGENTS.md`

Follow all instructions in that file for every task in this workspace.
